raise ImportError(
    "This example uses the legacy BSSNN API (bssnn.config, bssnn.training, etc.) "
    "which has not been migrated to the CISSN package. "
    "Run 'uv run examples/demo_cissn.py' for the current CISSN workflow."
)
